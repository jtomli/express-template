<div class="panel panel-default">
 <!-- Default panel contents -->
 <div class="panel-heading">{{venue.name}}</div>
 <div class="panel-body">
   <div>{{venue.name}} is located at {{venue.address}}.</div>
   <!-- map? -->
   <!-- {{# each venue.photos}}
    display the photos
   {{/each}} -->
   <button type="button" class="btn btn-primary">Add to Email List</button>
 </div>
</div>
